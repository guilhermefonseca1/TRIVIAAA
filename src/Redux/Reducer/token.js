const INITIAL_STATE = {
  response_code: 0,
  response_message: '',
  token: '',
};

const player = (state = INITIAL_STATE, action) => {
  switch (action.type) {
  default:
    return state;
  }
};

export default player;
